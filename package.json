{
  "name": "lia-coordinate",
  "version": "0.0.1",
  "description": "LiaScript plugin for interactive coordinate systems using JSXGraph",
  "scripts": {
    "build": "npm run build:full && npm run build:static",
    "build:full": "parcel build src/index.ts --dist-dir dist --no-scope-hoist",
    "build:static": "parcel build src/static.ts --dist-dir dist --no-scope-hoist",
    "test": "node --test tests/macroLayer.test.mjs tests/combinedQuiz.test.mjs tests/quadrilateralForm.test.mjs tests/constructionQuiz.test.mjs tests/quizMacros.test.mjs tests/area.test.mjs tests/dgsRegressionProfile.test.mjs tests/lineStyle.test.mjs tests/staticSvg.test.mjs",
    "test:browser": "npm run build && node --test tests/staticPerformance.browser.mjs"
  },
  "devDependencies": {
    "parcel": "^2.13.3",
    "typescript": "^5.8.3"
  },
  "targets": {
    "main": false,
    "default": {
      "distDir": "dist",
      "context": "browser"
    }
  }
}
